'use client';

import { Title } from '@/components/Title';

export default function Home() {
  return (
    <main className="h-full flex justify-end z-10 relative">
      <Title />
    </main>
  );
}
